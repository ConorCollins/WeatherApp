<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container d-flex justify-content-center">
        <!-- Navbar Brand (Centered) -->
        <a class="navbar-brand fw-bold text-center text-light" href="#">Weather Menu</a>

        <!-- Navbar Toggler (for mobile view) -->
        <button class="navbar-toggler position-absolute end-0 me-3"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</nav>



<!-- Navigation Menu -->
<div class="@NavMenuCssClass nav-scrollable bg-dark vh-100">
    <nav class="nav flex-column p-3">
        <!-- Home Link -->
        <div class="nav-item mb-3">
            <NavLink class="nav-link d-flex align-items-center text-light" href="" Match="NavLinkMatch.All">
                <span class="me-2" aria-hidden="true"></span>
                <strong>Home</strong>
            </NavLink>
        </div>

        <!-- Forecast Link -->
        <div class="nav-item mb-3">
            <NavLink class="nav-link d-flex align-items-center text-light" href="forecast">
                <span class="me-2" aria-hidden="true"></span>
                <strong>3 Day Forecast</strong>
            </NavLink>
        </div>

        <!-- Weather Link -->
        <div class="nav-item">
            <NavLink class="nav-link d-flex align-items-center text-light" href="weather">
                <span class="me-2" aria-hidden="true"></span>
                <strong>Current Weather</strong>
            </NavLink>
        </div>
    </nav>
</div>


@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}

